/// Application layer — use cases orchestrate the domain repositories.
///
/// Each file implements a single use case as a callable class that never
/// imports Flutter and never talks to the infrastructure directly.
library;
